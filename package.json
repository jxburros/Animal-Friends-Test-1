{
  "name": "animal-friends-tcg",
  "version": "0.2.0",
  "private": true,
  "description": "Animal Friends TCG: First Boroughs — browser game, headless engine and playtest simulator",
  "type": "module",
  "scripts": {
    "serve": "python3 -m http.server 8080",
    "test": "node --test test/",
    "smoke": "node scripts/smoke.mjs",
    "invariants": "node scripts/invariants.mjs 200",
    "playtest": "node scripts/playtest.mjs"
  },
  "engines": { "node": ">=22" }
}
